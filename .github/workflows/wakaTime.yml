name: WakaTime Coding Stat
on:
  push:
    branches:    
        - master
  schedule:
        - cron: '0 21 * * *'

jobs:
  update-readme:
      runs-on: ubuntu-latest
      steps:
      - uses: athul/waka-readme@master
        with:
          WAKATIME_API_KEY: ${{ secrets.WAKA_TIME }}
